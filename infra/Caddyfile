# Replace patiently.example.com with your actual domain for your deployment.
# For local testing, use `localhost` and Caddy will serve over HTTP only.

{$DOMAIN:localhost} {
    encode gzip

    @api path /api/* /health
    handle @api {
        reverse_proxy api:8000
    }

    handle {
        reverse_proxy web:3000
    }
}
